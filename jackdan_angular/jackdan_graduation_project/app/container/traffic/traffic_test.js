'use strict';

describe('myApp.traffic module', function() {

  beforeEach(module('myApp.traffic'));

  describe('traffic controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var trafficCtrl = $controller('TrafficCtrl');
      expect(trafficCtrl).toBeDefined();
    }));

  });
});