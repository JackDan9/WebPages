'use strict';

describe('myApp.cater module', function() {

  beforeEach(module('myApp.cater'));

  describe('cater controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var caterCtrl = $controller('CaterCtrl');
      expect(caterCtrl).toBeDefined();
    }));

  });
});